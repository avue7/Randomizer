So I created the randomizer function to get the random number. Then I store this number a variable called secret. When other classes need this number than they can easilyjust get the secret by calling the get function for getting the secret. 
